#!/usr/bin/env bash
echo "glab $*" >> "$STUB_LOG"
if [ -n "${STUB_FAIL:-}" ]; then echo "HTTP 502 bad gateway" >&2; exit 1; fi
case "$1" in
  issue) cat "$GLAB_ISSUE_FIXTURE" ;;
  api)   cat "$GLAB_NOTES_FIXTURE" ;;
esac
