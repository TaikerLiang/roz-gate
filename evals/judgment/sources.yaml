# The judgment tier's source repository — private, so never copied in here.
# `python3 evals/judgment/sources.py` clones it to admc_path (relative to
# this directory, gitignored) or fetches it there; ROZ_GATE_ADMC overrides
# the path, e.g. to reuse an existing checkout. Runs clone their sandbox
# from that local clone — never the network.
admc_remote: git@github.com:emilyorz/ADMC.git
admc_path: .sources/ADMC
