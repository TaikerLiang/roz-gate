# Fixture: a measured tag whose scope one-liner wraps — a PRESENT tag,
# never to be read as absent by a checker that cannot parse it fully.

- **R2 · Lookup stays under 50ms** — *(measured 2026-08-20, 15 runs, dev db
  n≈10k, cold cache, single writer; harness linked from the spec CR)*.
