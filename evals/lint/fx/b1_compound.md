# Fixture: the B1 defect channel — evidence tag merged into an authority tag.

- **R4 · Offer expiry is enforced at read time** — *the DB already rejects
  expired rows at the storage layer (from Q4, unverified), so the API only
  mirrors it.*
