# Fixture: the compound defect AND an 80-column wrap after the comma.

- **R4 · Offer expiry is enforced at read time** — *the DB already rejects
  expired rows at the storage layer under load and contention (from Q4,
  unverified), so the API only mirrors it.*
