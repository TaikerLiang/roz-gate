# Fixture: a demoted empirical claim whose free-text risk wraps at 80 cols.

- **R7 · Lock contention stays below the retry budget** — *(assumed-empirical:
  under peak concurrent writes the row-lock wait may exceed the client
  timeout, which no one has produced in a test environment yet)*.
